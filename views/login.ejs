<%- include('partials/header') %> 

    <section class="container">
        <div class="form-container login-form">
            <div class="image-container">
                <h1>Login</h1>
            </div>
            <div class="form-group">
                <p>Hello, welcome back.</p>
                <form action="/campgrounds" method="POST">
                    <input type="text"name="Campground[image]" id="" placeholder="Enter username">
                    <input type="password" name="Campground[name]" placeholder="Enter password">
                    <button class="btn btn-form" href="/campgrounds">Login</a> </button>
                </form>
                <div>
                    <ul class="account-opt">
                        <li>Don't have an account? <span><a href="#">Create account.</a></span></li>
                        <li><a href="#">Forgot password?</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </section>

<%- include('partials/footer') %> 
